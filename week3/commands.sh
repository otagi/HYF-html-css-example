# This is a shell script file.
# It's a simple text file with one bash command on each line.
# To run it, use the "sh" command with the script filename as argument.
cp src
cd /some/path/that/does/not/exist
ls
